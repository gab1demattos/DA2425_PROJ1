#include "Routes.h"
#include "Graph.h"

template<class T>
void bestRoute(Vertex<T> *src, Vertex<T> *dest) {
    // to do
}

template<class T>
void restrictedRoute(Vertex<T> *src, Vertex<T> *dest, vector<int> &avoidNodes, vector<pair<int, int>> &avoidSegments, Vertex<T> *includeNode) {
    // to do
}

template<class T>
void environmentalRoute(Vertex<T> *src, Vertex<T> *dest, int &maxWalkTime, vector<int> &avoidNodes, vector<pair<int, int>> &avoidSegments) {
    // to do
}